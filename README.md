# mruby-zig

Embed [mruby](https://mruby.org) 4.0 in Zig applications — first-class,
safe by default, and built entirely with `zig`. One `zig build` fetches the
mruby 4.0.0 source (hash-pinned), generates its presym tables and core
bytecode, compiles everything with `zig cc`, and hands you a `mruby` module.
**No Ruby, no rake, no submodules, no system dependencies.**

```
 ┌──────────┐   presym scan (zig cc -E)     ┌─────────────────────┐
 │ mruby    │ ────────────────────────────► │ id.h / table.h      │
 │ 4.0.0    │                               └─────────────────────┘
 │ (zon dep)│   host mrbc (zig cc)          ┌─────────────────────┐
 │          │ ────────────────────────────► │ mrblib.c, gem_init  │
 └──────────┘                               └─────────┬───────────┘
                                                      ▼
                                      mruby module (C + Zig + shim)
```

## Status

Pre-release. Pinned to **Zig 0.17.0-dev.1978+c961124d9** via
[mise](https://mise.jdx.dev). Trusted and semi-trusted embedding is the
supported use; see the [threat model](docs/sandboxing.md) for why hostile
input needs the planned out-of-process tier. Supported platforms:
[docs/platforms.md](docs/platforms.md).

## Quickstart

```sh
mise install                 # install the repository's pinned Zig snapshot
mise x -- zig build test     # unit + Ruby integration suites
mise x -- zig build run-quickstart
```

```zig
const mruby = @import("mruby");

const vm = try mruby.Vm.init();
defer vm.deinit();

const result = try vm.loadString("[1, 2, 3].map { |x| x * x }.sum");
std.debug.print("{d}\n", .{try result.asInt()});
```

The entire C library is compiled into the module — consumers link nothing
else and need no Ruby toolchain at build time.

## Documentation

| Document | Contents |
| --- | --- |
| [getting-started.md](docs/getting-started.md) | dependency setup, build options (gems, allocator), feature manifest, dev commands, running CI locally |
| [safe-api.md](docs/safe-api.md) | embedding API: evaluation, exceptions, host functions, data wrapping, collections, lifetimes and roots, threading |
| [sandboxing.md](docs/sandboxing.md) | policies and presets, gas/deadline/memory limits, termination, lifecycle and sealing, threat model |
| [artifacts.md](docs/artifacts.md) | typed RITE images, state capsules, compatibility policy |
| [platforms.md](docs/platforms.md) | support matrix and CI coverage |
| [maintenance.md](docs/maintenance.md) | the Zig build of mruby, the audited hash patch, upgrade and release procedures |

Runnable examples (built and executed in CI):
[quickstart](examples/quickstart.zig) — embed and evaluate;
[host_functions](examples/host_functions.zig) — Zig methods with typed
arguments, blocks, and wrapped state; [exceptions](examples/exceptions.zig)
— raise/rescue across the boundary; [sandbox](examples/sandbox.zig) —
restricted policy, bootstrap + seal, limits, host access.

## Highlights

- **Safe embedding layer**: every allocating or raising operation is
  fallible; Ruby exceptions never longjmp across Zig frames; `Value`/
  `Class` handles are VM-owned and cross-VM use is rejected. Method
  signatures derive from Zig callback parameter types, so marshalling and
  arity cannot disagree.
- **Sandboxing**: deny-by-default capability policies with
  `Policy.trusted`/`Policy.restricted` presets, deterministic instruction
  gas, wall-clock deadlines, memory caps, call-depth limits, thread-safe
  termination with guaranteed unwinding, and an explicit bootstrap/seal
  boundary.
- **Typed artifacts**: compile-once RITE images with build-compatibility
  fingerprints and application identity; inert state capsules that move
  value graphs across processes without executing guest code.
- **Generated build**: presym tables, core bytecode, and gem registry all
  generated by `build.zig` — the rake build reproduced in Zig, with one
  audited core patch (see [maintenance.md](docs/maintenance.md)).
- **Comptime feature manifest**: `mruby.features` exposes gems, versions,
  compatibility identity, and target constraints for ordinary `comptime`
  branching.

## Layout

```
build.zig            # the entire mruby build, in zig
build/{sources,gems,gen}.zig
tools/               # presym_gen, artifact_config_gen, patch_mruby_hash, ...
src/c.zig            # hand-written extern bindings (public for power users)
src/shim.c           # C-side accessors for macro-only inline APIs
src/vm.zig           # Vm: eval, call, classes, globals, raise, arena
src/{value,class,data,convert,error,output,arena,alloc,sandbox,features}.zig
src/tests_ruby/*.rb  # Ruby-level integration suite (embedded, asserted in zig)
examples/            # quickstart, host_functions, exceptions, sandbox
tools/repl.zig       # `zig build run-repl -- -e 'expr'` eval tool
```

## License

MIT — see [LICENSE](LICENSE). mruby is MIT and fetched at build time as a
pinned, hash-verified dependency.
